# 基于强化学习的公共政策多跳问答 RAG 系统优化研究

2025.05 - 2025.07

- **S｜项目背景**：针对英国公共政策咨询中年龄、收入、家庭关系、居住地及时间等约束密集，用户表达与政策条款结构不一致，直接检索易遗漏前置条件、召回相似但不适用条款的问题，定位 RAG 链路瓶颈为 Query Planner 的约束保真与多跳规划能力。
- **T｜项目目标**：基于 Qwen3-8B-Instruct 构建可生成结构化 JSON 查询计划的 Planner，将上下文相关问题改写为独立检索 Query，并将复杂政策问题拆解为 2～4 跳、带依赖关系的查询 DAG；重点对比 DPO 与 GRPO 两种训练方案对约束保真、检索召回和端到端问答效果的影响。
- **A｜数据工程**：整合 ConditionalQA、QReCC 与 MuSiQue，构建 12,552 个英国政策知识块及 117,528 条多跳辅助语料；保留 20,000 条单跳基础 SFT，额外构建 2,000 条 MuSiQue 多跳 SFT 冷启动数据（2/3/4-hop 为 1,000/600/400），并从剩余 MuSiQue 样本中抽取 5,000 条 GRPO-ready 数据，通过来源 ID 与问题指纹保证两者零重叠；同时构建 5,000 组 DPO 数据及 5 类均衡困难负样本。
- **A｜训练方案**：设计“单跳基础 SFT → 多跳 SFT 冷启动 → 共享检查点 → DPO/GRPO”训练路径；DPO 通过 5 类困难负样本强化实体及政策约束保真，GRPO 计划以格式合法性、约束覆盖率、依赖正确性和检索收益构建组合奖励，两条分支从同一冷启动权重出发以控制变量。
- **A｜系统评测**：设计 BGE-M3 + FAISS 检索与答案生成链路，在 ConditionalQA Dev、QReCC Test 和 MuSiQue Dev 上统一评测基础 SFT、冷启动、DPO、GRPO 四个检查点，并按 2/3/4-hop、约束类型及负样本错误类型开展消融分析。
- **R｜项目进展**：已完成知识库、训练数据与泄漏校验流水线；模型训练、检索联调及 Gold-document/Evidence Recall、Joint Recall、答案 F1 等实验结果均为 `[待测]`，待训练日志、模型权重和评测产物完整后再报告。
