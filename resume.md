### 基于强化学习的公共政策多跳问答 RAG 系统优化研究

2025.05 - 2025.07

- **S｜项目背景**：针对英国公共政策咨询中年龄、收入、家庭关系、居住地及时间等约束密集，用户表达与政策条款结构不一致，直接检索易遗漏前置条件、召回相似但不适用条款的问题，定位 RAG 链路瓶颈为 Query Planner 的约束保真与多跳规划能力。
- **T｜项目目标**：基于 Qwen3-8B-Instruct 构建可生成结构化 JSON 查询计划的 Planner，将上下文相关问题改写为独立检索 Query，并将复杂政策问题拆解为 2～4 跳、带依赖关系的查询 DAG；重点对比 DPO 与 GRPO 两种训练方案对约束保真、检索召回和端到端问答效果的影响。
- **A｜数据工程**：整合 ConditionalQA、QReCC 与 MuSiQue，构建 12,552 个英国政策知识块及 117,528 条多跳辅助语料，完成 20,000 条 SFT、5,000 组 DPO 和 5,000 条 GRPO 训练数据；设计指代未消解、实体遗漏、关键约束遗漏、查询过宽和错误上下文 5 类均衡负样本，并通过 Schema 校验、固定随机种子、数据指纹与 benchmark 泄漏检测保证可复现性，政策证据映射率达到 99.99%。
- **A｜训练方案**：采用 LoRA 对同一 SFT 初始化权重分别开展 DPO 与 GRPO 训练，保持基座模型、训练数据量和推理参数一致以控制变量；DPO 通过 5 类难负样本强化实体及政策约束保真，GRPO 则以格式合法性、约束覆盖率、依赖 DAG 正确性、Gold-document/Evidence Recall 和最终答案 F1 构建组合奖励，直接优化多跳检索的实际收益。
- **A｜系统评测**：搭建 BGE-M3 + FAISS 检索与答案生成链路，在 ConditionalQA Dev（285 条）、QReCC Test（16,451 条）和 MuSiQue Dev（2,417 条）上对 SFT 基线、DPO 和 GRPO 进行统一评测，并按 2/3/4-hop、约束类型及负样本错误类型开展消融实验，分析两种训练方案的能力边界。
- **R｜项目结果**：DPO 在单跳政策检索上表现更优，将 Gold-document Recall@5 从 SFT 基线的 78.9% 提升至 83.6%，Evidence Recall@5 从 68.2% 提升至 74.9%；GRPO 在复杂多跳场景中优势更明显，将 Joint Recall@5 从 50.1% 提升至 64.5%，端到端答案 F1 从 60.5% 提升至 68.7%，同时保持 99.8% 的 Planner JSON 合法率。
